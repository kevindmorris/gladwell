import { kebab, lower, snake, upper } from "../utils";

class gladwell {
  static kebab(e: string) {
    return kebab(e);
  }
  static lower(e: string) {
    return lower(e);
  }
  static snake(e: string) {
    return snake(e);
  }
  static upper(e: string) {
    return upper(e);
  }
}

export default gladwell;
